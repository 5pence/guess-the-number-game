<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#linkToPlay"
          th:href="@{__${T(spencer.barriball.util.GameMappings).PLAY}__}"
          th:text="Play"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
</thlogic>