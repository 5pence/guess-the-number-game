<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#resultMessage"
          th:text="${__${T(spencer.barriball.util.AttributeNames).RESULT_MESSAGE}__}"/>
    <attr sel="#restart"
          th:href="@{__${T(spencer.barriball.util.GameMappings).RESTART}__}"/>
    <attr sel="#linkToHome"
          th:href="@{__${T(spencer.barriball.util.GameMappings).HOME}__}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
</thlogic>